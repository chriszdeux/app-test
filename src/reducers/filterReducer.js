import { types } from "../types/types";

const { filter_posts } = types

// export const filterReducer = ( state = [], action ) => {
//   switch (action.type) {
//     case filter_posts:
//       debugger
  
//     default:
//       return state;
//   }
// }